{
  "name": "dao-yu-101",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && git add . && git commit -m \"Deploy to GitHub Pages\" && git push origin main --force"
  },
  "dependencies": {
    "astro": "^5.18.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@astrojs/react": "^0.1.0"
  }
}
